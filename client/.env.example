# Backend API base URL. Leave blank for local dev - Vite proxies /api to
# http://localhost:5000 (see vite.config.js). Set this when the frontend is
# deployed separately from the backend, e.g.:
# VITE_API_URL=https://your-backend.onrender.com/api
VITE_API_URL=
